# Reduced-privilege netdata: apps.plugin (per-process metrics) can't read other
# PIDs' /proc without SYS_PTRACE + pid:host, which we don't grant. Left on it
# only spams "Permission denied". Disable it; proc/cgroups/go.d/health stay on.
[plugins]
    apps = no
